name: Build Result image

on:
  push:
    branches: ['qa']

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3


      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: issiwara
          password: AL=50941097

      - name: Build the Docker image
        run: docker build . -f Dockerfile --tag issiwara/advansed_web_task_3

      - name: Docker Push
        run: docker push issiwara/advansed_web_task_3